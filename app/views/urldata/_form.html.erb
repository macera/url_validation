<%= form_for(@urldatum) do |f| %>
  <% if @urldatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@urldatum.errors.count, "error") %> prohibited this urldatum from being saved:</h2>

      <ul>
      <% @urldatum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
